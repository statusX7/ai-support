# v1.1.1 实现与验收报告

本文件随源码冻结；远端发布、最终资产 SHA-256 和匿名安装的完成事实，以同一 Release 的独立附件 `v1.1.1-verification.md` 为准。发布后不移动 tag 来写回自引用哈希。

## 1. 基线与版本

- 开发目录：`/root/projects/crispai`；现有仓库：`statusX7/ai-support`。
- 实施基线：`05ac887ce46ba86780ba1c8d4b30283abee9de8d`；v1.1.0 冻结 tag：`4ddcf6e6129e8d8d1b6314825fc80bdb6998ddd1`。
- 唯一目标：v1.1.1。最终源码通过 `git rev-parse 'v1.1.1^{commit}'` 解析，验收 commit 与资产关系记入独立回执。
- 本轮核对时仓库已经公开，默认 GitHub CLI 身份具有管理权限，原 SSH origin 保持不变。历史报告中的 private 是当时事实，不改写。
- 基线主驱动：51 通过、0 失败、2 跳过（STATIC 8、UNIT/CONTRACT 43；未指定真实本地实例及缺外部账户各跳过 1）。此数字仅为本轮修改前基线，不与新版本结果累加。

## 2. 实际协作与所有权

| 执行者 | 独占实现与实际职责 |
| --- | --- |
| Agent A `online_installer` | 独立 get.sh、下载协议/PTY 测试、README/INSTALL；菜单 14 匿名更新；旧快照可选文件兼容；准备独立 v1.1.0 升级 VM |
| Agent B `component_doctor` | 统一 doctor/healthcheck、结构化结果、限时只读检查、安全 fix、故障 fixture |
| Agent C `public_acceptance` | 独立复验、公开历史与资产审查、18 菜单文档核对；准备全新 Debian 12 VM 和真实故障脚本 |
| 主 Agent | 公共模块与生命周期清单、心跳接线、版本/工作流生成、全量回归、真实安装集成、最终报告、打包、GitHub 发布与匿名实装 |

公共文件按所有权交接，未并发覆盖 common/Compose/workflow。中断恢复后 C 遇到线程总数限制，主 Agent 接手并修正其真实故障脚本、实际执行故障注入；没有伪造 C 在恢复后继续运行。Agent 的通过报告不替代主 Agent 集成复验。

## 3. 需求追踪

| 本轮目标 | 生产入口/实现 | 运行时事实与验证 | 验收 ID |
| --- | --- | --- | --- |
| 一条命令安装 | README/INSTALL 的同一命令；get.sh 的版本解析、下载、安全解包、run_package_entry | 取完整同 tag 包后调用现有十项 install.sh；来源写入 config/.online-release；原 cwd/真实 TTY 保留 | I01–I14、P03–P05 |
| 组件自检 | manage.sh status_menu；scripts/doctor.sh；healthcheck.sh 兼容封装 | 当前容器/应用身份、配置读回、生产 Code 拒绝路径、scheduler 心跳；logs/doctor-last.json | D01–D16 |
| 核心缺陷与回归 | 公共清单、rollback.sh、runtime.js、get.sh；18 菜单保持 | 同会话代次、旧快照、敏感配置和索引不被自检改变 | R01–R07 |
| 持续公开与正式分发 | AGENTS.md、RELEASE.md、package-release.sh、public_distribution 测试 | public、非 draft/prerelease、Latest、匿名 repo/raw/tag 资产及原样命令 | P01–P07 |

## 4. 在线入口与新手流程

冻结的唯一推荐命令如下，与 [INSTALL.md](../INSTALL.md)、README 和本版 Release notes 逐字一致；前三处由自动测试核对，本报告副本在冻结前再作字节比对。

```bash
bash -c 'set -euo pipefail; u=https://raw.githubusercontent.com/statusX7/ai-support/main/get.sh; t=$(mktemp /tmp/crispai-get.XXXXXXXX); cleanup(){ r=$?; trap - EXIT; rm -f -- "$t"; exit "$r"; }; trap cleanup EXIT; f=; if [[ -s /etc/ssl/certs/ca-certificates.crt ]] && command -v curl >/dev/null 2>&1; then f=curl; elif [[ -s /etc/ssl/certs/ca-certificates.crt ]] && command -v wget >/dev/null 2>&1; then f=wget; else command -v apt-get >/dev/null 2>&1 || { printf "错误：当前系统无法自动安装安全下载工具。\n" >&2; exit 1; }; p=(); if (( EUID != 0 )); then command -v sudo >/dev/null 2>&1 || { printf "错误：需要 root 或 sudo 补齐安全下载工具。\n" >&2; exit 1; }; p=(sudo --); fi; "${p[@]}" env DEBIAN_FRONTEND=noninteractive apt-get -q -o APT::Color=0 -o Dpkg::Use-Pty=0 -o DPkg::Lock::Timeout=180 -o Acquire::Retries=3 -o Acquire::http::Timeout=30 -o Acquire::https::Timeout=30 update; "${p[@]}" env DEBIAN_FRONTEND=noninteractive apt-get -q -o APT::Color=0 -o Dpkg::Use-Pty=0 -o DPkg::Lock::Timeout=180 -o Acquire::Retries=3 -o Acquire::http::Timeout=30 -o Acquire::https::Timeout=30 install --yes --no-install-recommends ca-certificates curl; f=curl; fi; if [[ "$f" == curl ]]; then curl -q --fail --location --silent --show-error --max-redirs 5 --proto "=https" --proto-redir "=https" --tlsv1.2 --retry 3 --retry-delay 2 --connect-timeout 15 --max-time 300 --output "$t" "$u"; else wget --no-config --no-netrc --https-only --secure-protocol=TLSv1_2 --timeout=30 --tries=3 --output-document="$t" "$u"; fi; e=; while IFS= read -r l || [[ -n "$l" ]]; do e=$l; done < "$t"; [[ -s "$t" && "$e" == "# crispai-get-end" ]] && bash -n "$t" || { printf "错误：get.sh 下载不完整或语法无效。\n" >&2; exit 1; }; chmod 0700 "$t"; bash "$t"'
```

命令先检查 curl/CA，必要时利用已有 wget 或受控 apt 补齐；下载至随机受限文件，检查完整结束标记及 Bash 语法后执行并保留退出码。

get.sh 不预先读取邻接模块。默认只解析一次具体稳定 tag，核对正式 Release 元数据，再固定下载归档与单目标 SHA256SUMS；拒绝 HTML、路径穿越、重复成员、链接/特殊文件、超限内容及版本混用。校验清单是同源完整性证据，不是独立发布者签名。

同版本默认进入管理菜单；旧版不静默升级；`--update` 使用目标包的维护逻辑；中断与安全卸载复用持久资料；非项目非空目录拒绝写入。无交互终端非零退出。普通用户通过受控 sudo 进入，不传递失效 stdin FD。

快速初始化依旧是 API 地址、隐藏 Key、模型、Website ID、隐藏 Identifier、隐藏 Crisp Key、公网地址、Prompt、知识、数字确认共十个主步骤。文件路径的正常输入为 10 项，确认后不得新增内部参数提问。Prompt/多库扩展输入不伪称十次按键。

## 5. 自检语义

菜单 2：快速、完整、缓存结果、安全修复、脱敏导出。`crispai doctor` 默认不推理、不修复；`--local` 不发外部业务请求；`--full` 显式授权少量合成推理；`--json` stdout 只含 JSON；`--fix` 只执行白名单后复检。总预算默认 90 秒，单项卡住仍汇报其余检查。

结果逐项包含 id、中文名称、状态/严重性、摘要、时间、耗时、来源/范围、修复建议。退出码 0/2/1 分别为无问题、警告待接入、明确故障；64 参数错误、130 中断。安装/回滚的兼容健康入口将非关键外部待接入与必要组件故障分开处理。

检查不发送客户消息、不刷新全部知识索引、不调整总开关或人工/offer/截止时间。scheduler 心跳由现有定期扫描主动维护，而不是借客户流量判断；默认 doctor 只读它。失败恢复或未完成安装状态不允许 fix 擅自启动混合代际服务。

## 6. 公开前审查范围

本轮独立审查已覆盖 47 个 commit、408 个 blob、637 个可达 Git 对象，远端 main 与三个历史 tag，三个 Release 的说明及现存资产；当前实际 Actions runs/artifacts、Issues/PR/comments、部署/环境均为空。历史资产校验和与原回执一致；未发现已知真实凭据、用户知识或客户会话。受限测试配置仅用于本地精确对照，不上传。

上述数量对应独立审计检查点，不冒充最终提交数量。主 Agent 在 `ceccb38` 再次扫描当前跟踪内容、未忽略文件和全部可达历史：52 个 commit、744 个可达对象，已知秘密模式门禁通过；同时复核远端仍只有 main 与三个历史 tag、Actions runs/artifacts 和 Issues/comments 仍为空。最终报告提交、tag 和实际发布资产的增量复核写入独立回执。

Linked ProjectsV2 读取需要当前凭据未授予的 `read:project`，该部分未检查；不因此声称完成全账户安全认证，也不擅自扩大 Token scope。仓库历史扫描不是对外部账户全部资料的保证。

公开的是源码，不是部署目录、.env、Prompt/知识、数据、日志或备份。所有本轮未公开原始证据在已忽略且受限的 `.work/v1.1.1/`。

## 7. 已确认缺陷与修复

以下是本轮实际发现的问题，不把任务书中的风险清单直接当成缺陷。

| 编号 | 文件 / 函数 | 复现与根因 | 修复与本轮证据 |
| --- | --- | --- | --- |
| B01 | get.sh；README/INSTALL | 原 install.sh 依赖邻接模块，不能单文件线上执行；新手仍需手动取得完整包 | 独立获取器先锁定/校验完整归档再调用 install；18 组下载/PTY 契约，真实 VM 取包进入向导 |
| B02 | manage.update_rollback_menu | 菜单更新仍依赖手动解压目录，公开安装与日常更新不闭环 | 菜单 14→1 复用 get --update；同版无操作、旧版明确授权才升级、失败保留现有实例 |
| B03 | healthcheck.sh、doctor_run_checks | 旧诊断会执行模型请求，缺少多组件当前运行代的分项证据 | 统一只读引擎；默认不推理，full 才调用；检查唯一 ID、当前配置、严格结果与截止时间 |
| B04 | doctor_container_env_binding / external probes | 健康接口可通但容器仍保留旧 Key/模型/Crisp 环境；旧 observation 不能代表新凭据 | 容器内只返回 matched，不输出秘密；七字段 observation 绑定，401/403 明确故障；D08/D09 契约 |
| B05 | n8n/build-workflow.js、doctor_n8n_check | 首轮真实安装中，将已规范化嵌入代码 hash 与 runtime 原文件 hash 混比，合法工作流被拒绝 | 保留 runtimeSha256，增加 runtimeFileSha256，核对四个实际 Code 前缀、版本、active、扫描连接；生产导出与 Code 探针复验 |
| B06 | doctor_n8n_check / doctor_adapter_check | SSH 非 TTY 通过，真实 PTY 中两个 Docker exec 探针固定超时；timeout 新进程组仍读取终端 stdin，触发 SIGTTIN | 仅不需要输入的探针重定向 /dev/null；密码/环境比对仍走受控 stdin；真实 PTY 续装 64.43 秒、0 输入、0 ANSI；新增主动读取 stdin 的 PTY fixture |
| B07 | doctor_adapter_check | 未选的 Hook Secret 在宿主为空、Compose 展开为 not-configured，合法单 Hook 模式误报偏离 | 只对未选字段按 Compose 默认归一化，选中 Secret 仍必须有效；Website/Plugin 两路径契约 |
| B08 | doctor_db_check | REAL D05 注入错误 POSTGRES_PASSWORD，旧探针仍 PASS；真实 pg_hba_file_rules 显示 loopback 使用 trust | 改走 backend 服务名 postgres 做密码认证与 SELECT 1。注错时 database.authentication / n8n_binding FAIL，恢复原 .env hash 后 PASS；d05-postgres-auth-bridge.json |
| B09 | doctor_anything_check | catalog 有启用文档，但 manifest 和 workspace 都为空时，两个空 location 集可能假 PASS | 逐文档检查同 hash、非空 location；完整已索引或完整 pending 才有效；缺失/无关 pending 为 FAIL，同 hash 正常 pending 为 WARN，未重建索引 |
| B10 | snapshot.sh / rollback.sh / get.required_package_entries | 真实 v1.1.0 快照没有 get/doctor；新清单强求新文件使旧代恢复失败 | 必需旧文件与可选新模块分开；先校验再停机，回滚删除目标版本不存在的本项目新模块。真实 v1.1.0 升级/手动回滚/再升级与冷恢复通过 |
| B11 | rollback.sh / restore.sh | 统一自检只接安装/更新、不接恢复完成状态，恢复后缺少同等级接线门禁 | 新版恢复提交状态前调用同一 application 门禁；旧版没有 doctor 时保留其已验证路径。故障后继续保持应用停止、保留安全快照，不启动混合代际 |
| B12 | doctor_safe_fix、超时与依赖检测 | staged 代不能被修复入口启动；缺 jq/base64 或卡住请求不得无报告退出/沿用绿灯 | 先检查稳定代，安全白名单独立预算；缺依赖、挂起 API、JSON、64/130、非零菜单返回分别验证 |
| B13 | menu-ui.sh、manage.status_menu、package-release.sh | 菜单缺带时间的诊断缓存；新增 df fixture 不在固定打包清单 | 显示缓存时间/计数；补入明确清单。38 组生产菜单 PTY 与归档缺开发缓存测试 |
| B14 | doctor_files_and_config_check 的 launcher 分支 | 已有正常实例时，新安装显式指定自定义 command-path；健康门禁早于入口 marker 创建，却提前把默认路径上的既有入口当作冲突 | 仅安装中且本实例尚无入口记录时跳过未知入口；有记录的错误归属仍 FAIL。32 组 doctor 专项及真实 VM 完整主驱动 57/0/1 通过，原 wrapper 哈希/归属未变；最终入口的路径/归属/数字冲突处理未改 |
| B15 | runtime.knowledgePlan / transcript / expire | 视觉摘要进入当次 RAG，但普通合法答案未复述细节时，下一问仅还原附件占位符和公开答案；显式 reset 后必要图片内容丢失 | 主 Agent 的隔离 UNIT 反例先失败，补可选 image_context 后通过；按会话匹配近期附件，最多3份/2000字符/24小时，不存URL或bytes。新增重启/限量/过期/跨会话及迟到解析双重代次测试；真实图片链路与最终复验记录见发布回执 |

同时修正两类测试设置，未放宽生产断言：离线业务恢复会故意失效实例索引映射，正常回滚测试先执行生产同步并回读再快照；构造旧配置测试不保留当时尚不存在的新 doctor/get。此前健康门禁拒绝这些不一致 fixture 是正确结果。最终生命周期选定子集 35/35；最终全量另计。

检查后未发现需要重写的路径：v1.1.0 的 BASHPID/FD 维护锁重入、严格 .env 解析、URL 前缀/容器 loopback 映射、特殊字符隐藏输入、pending 重试、真实 n8n 沙箱、按钮/代次/永久人工、完整角色密码冷恢复、数字卸载和外部网络占用保护。它们保持原实现并参加回归，不把旧报告当本轮新增通过。F03/F08/F09/F11/F12/F16/F17/F18/F19/F20/F21 分别由 Provider、配置护栏、真实生命周期、Crisp 响应、工作流、空机/PTY、observation、失败恢复、模型列表、干净 clone 和纯文本测试覆盖。

## 8. 菜单、配置与实际消费者

保留 18 个固定主菜单；所有分支经生产 manage.sh / wrapper 的 PTY 遍历，新增 self-check 与匿名更新另有专项。

| 编号 | 实际函数 / 程序 | 落盘与运行时消费者 / 生效范围 |
| --- | --- | --- |
| 1 | quick_initialization → install.sh | 受限向导进度、.env、config、knowledge → 应用初始化与回读；现有配置不默认重置 |
| 2 | status_menu → doctor.sh | logs/doctor-last.json 白名单诊断；读取当前组件，不改业务 |
| 3 | ai_config_menu → provider_apply | .env / provider.yaml → adapter / AnythingLLM / n8n；成组验证，失败恢复 |
| 4 | prompt_menu → configuration_prompt_apply | prompt.md / 历史 → workspace API 与当前请求；旧会话下一问也生效 |
| 5 | knowledge_menu → knowledge_sync_catalog | catalog、各库原文、manifest → 单 workspace 实际文档/检索；禁用退出新检索 |
| 6 | rules_menu → configuration_apply | keyword.yaml → runtime.receive/process；只展示确认按钮 |
| 7 | handoff_menu → runtime-cli | handoff.yaml、按会话持久状态 → pause/expire/scan；0 永久 |
| 8 | global_switch_menu / enable / disable | runtime.yaml revision → 全局发送门禁；不停止容器、不解除人工 |
| 9 | welcome_menu / multilevel_menu | menu.yaml → 工作流及无密钥 SDK；欢迎与自动展开独立 |
| 10 | crisp_menu → crisp-settings.sh | .env、当前凭据绑定观察 → n8n/Crisp；显示真实 URL 前确认 |
| 11 | statistics_menu → analytics.sh | tags/feedback.yaml、受限统计 → 标签并集与反馈；差评不人工 |
| 12 | migration_menu → migration.sh | 不含秘密的完整业务包；包含所有库原文，验证/预览/应用/失败恢复 |
| 13 | backup_restore_menu | 本机完整备份含 .env/数据库/AnythingLLM/人工状态；受限保存、成套恢复 |
| 14 | update_rollback_menu → get --update | 固定公开包 → 一致性快照、更新门禁、失败回滚；同版不改实例 |
| 15 | diagnostics_menu / redact_logs | 限长脱敏日志与诊断导出；不输出完整 env 或客户正文 |
| 16 | services_menu / maintain_services | 已登记项目的依赖/容器/同步/入口维护；不清理宿主 Docker |
| 17 | help_menu | 已部署本地文档与公开 GitHub 链接；无网络也可读本地资料 |
| 18 | uninstall_menu → uninstall.sh | 数字确认安全卸载/完整清理；精确资源复核、外部受限备份与入口清理 |

客服默认仍为启用、欢迎启用、确认按钮启用、恢复 1800 秒、网页自动展开关闭。任意一次 doctor 或其安全 fix 不把管理员主动关闭的开关、无限人工或合法外部反代当作应“修好”的故障。

## 9. 真实 VM、依赖与生命周期

本轮只将 Debian 12 amd64 标为真实部署验证。其他受支持发行版由依赖契约覆盖，不冒称空机实测。

- 22244：新建 pristine Debian 12 qcow2 的独立 28 GiB overlay、4 GiB 内存；初始无 Docker/Compose/jq/Git/gh，未挂宿主 Docker socket。生产入口自行准备 Python/PyYAML、jq、cmp/flock 等工具，安装 Docker 29.8.0、Compose 5.5.1、containerd 2.3.4，daemon active + enabled，运行四个真实应用容器。
- 首次候选完整文件来源路径 10 项主要输入，最终确认后 0 额外；Prompt 与知识路径相对调用 cwd，三个秘密输入 echo 关闭。首轮最终自检失败未假报成功，保留 installing/输入；修复后 candidate4/candidate5 分别 0 输入续装/同版复用，candidate5 64.43 秒，退出 2、无泄漏/ANSI。
- 主 Agent 将最后核心修复 candidate6 的已校验完整归档再次通过生产 install.sh 复用安装：68.58 秒、0 主输入/0 额外，退出 2；秘密泄漏断言为 false，ANSI 为 0。没有绕过 get.sh 对同版本在线更新的无操作保护。
- Prompt 经真实 AnythingLLM API 逐字核对；MD/TXT/PDF/DOCX 四份有效合成文档已解析、索引、workspace 回读。revision 与 applied_revision 一致。从 `/`、`/tmp`、`/root` 调用受管 /usr/local/bin/crispai 均指向 /opt/crisp-ai。
- 22245：从冻结 v1.1.0 的真实实例建立隔离 overlay。候选2/3 的真实安装门禁失败触发自动回滚，四组件恢复；候选4 正常升级→手动回滚 v1.1.0→再升级全部完成，14 项自定义业务/秘密/知识/永久人工 hash 保持。
- 14 项保护包括：全局停用、欢迎关闭与自定义自动展开、0 秒、原 Prompt、两个命名库五份文档、五项秘密 hash、人工 A 的 mode/generation。人工 A 未解除，其他测试会话未被全局接管。
- 安全卸载输入 `1→1`：服务/网络/本人 wrapper 移除，配置/知识/数据库/人工/日志保留；同路径恢复无向导，14 项保持。完整清理输入 `2→1→1`：先产生部署目录外 0600 完整备份，再删本实例；Docker 与外部协议服务未动。
- 冷恢复另从当时真实公开 Latest v1.1.0 完成 10/0 新装，再升级候选并使用生产 restore --full 导入外部备份。角色密码、数据目录挂载、工作流、Prompt/索引和 14 项原状态全部恢复；不是仅新代码之间复制文件。
- 22246：另建真正全新公共分发 VM，初始 curl/wget/git/gh/jq/docker 均无。它专用于正式 Release 之后以普通 sudo 用户原样运行唯一推荐命令；完成事实和依赖前后清单写入独立发布回执，不能拿已登录 gh 下载或上述协议包替代。

宿主与 guest 曾有约八分钟时钟差，证据下载提示未来 mtime；没有修改无关宿主时间。所有真实秒数/截止验证在同一 guest 时钟内执行，不能据跨机器 mtime 判断提前恢复。测试 Node/Git/ShellCheck 在安装完成后为维护验收单独准备，不属于普通宿主运行前置。

受限原始证据：`.work/v1.1.1/clean-vm/`、`upgrade-vm/lifecycle/`、`real-doctor/`、`runtime-acceptance/`、`public-final-vm/`；不随源码或 Release 上传。

## 10. 自检故障与业务链路结果

主 Agent 执行真实故障脚本：11 项通过、0 失败，最终 local 自检 37 PASS / 0 WARN / 0 FAIL / 5 SKIP。SKIP 是未启用 Caddy 与 local 范围之外的外部检查，不是故障豁免。每次结果均重新读取，不引用缓存。

11 项分别为：默认只读基线；分别停止 PostgreSQL、AnythingLLM、n8n；停止 adapter 后显式 fix；错误数据库密码；错误 AnythingLLM Key；Prompt 偏离；修改并发布实际 n8n Code；阻断扫描写入/心跳停滞；staged 状态拒绝 fix。全部恢复后业务文件/会话 digest 与原值相等，默认检查不替换容器，fix 仅作用被明确停止的本项目 adapter。

机器摘要 `.work/v1.1.1/real-doctor/evidence/summary.txt`，各故障及恢复的脱敏 JSON 同目录；首次 D05 真正假绿失败日志保留为 `execution-candidate4-d05-failure.log`，修复后完整重跑为 `execution-candidate5.log`，不把失败轮次计入11。

真实业务链路完整一轮为 26 通过、0 失败：生产 n8n 事件/状态/出站故事 14，同一旧会话 Prompt 热应用 1，三库同时启用及三轮启停检索 9，Chat-only/Responses-only 最终调用 2。Agent B 完成候选5后，主 Agent 在 candidate6 的真实 22244 实例独立重新执行全部 26 项，退出 0、全部通过；两轮不累加。主 Agent 直接核对机器结果及清理结果：原 .env、Provider、Prompt、catalog、manifest、会话、统计及配置历史 hash 全部恢复，最终自检仍为 37/0/0/5。

三份虚构中文事实全部在三个库同时启用后，以不同表述分别获得正确首位来源；三轮停用后的首次查询都不再包含停用库，三轮重新启用后首次查询均恢复正确来源（attempts=1，约 0.25～0.32 秒）。更早的合成语料曾出现首位断言失败：当时读取的机器 JSON 转录显示电脑目标仍被召回但排第 2（score 0.203217），订阅排第 1（0.214886）；原始临时文件未保存，原因未证实，不能声称索引延迟或已修复向量引擎。后续使用语义区分更清楚的虚构中文事实，补齐所有来源排名/分数/耗时记录；不是更换生产 Embedder 或用重试掩盖丢失映射。向量相似度不等于答案正确率，也不保证任意真实语料都首位命中。

Prompt 测试核对下一次请求只含新 Prompt，并保留本会话先前公开上下文；不声称协议服务验证了模型遵循指令能力。两种 Provider 模式各观察到一次真实运行链路请求，分别止于上游 `/responses` 与 `/chat/completions`，而不是只检查能力探测。

所有真实本地消息实验使用隔离协议服务模拟 Crisp 与 Provider；PostgreSQL、n8n 生产工作流、AnythingLLM 和本地 Embedding 是真实组件。模拟 Provider 的固定回答不能证明真实模型知识回答质量；中文检索以实际向量返回的文档来源、排名与正文证据核验。

两层业务控制保持：关键词只展示 picker，未点击仍为 AI；合法点击先暂停本 conversation 再确认一次。真人公开回复立即递增该会话代次，在途旧回答丢弃；A 人工不影响 B。最近有效人工活动重置截止时间，访客/重复/乱序事件不重置，0 永久；5 秒持久扫描与新消息惰性校验共同恢复，重启不丢状态。不补答人工/全局停用期间的历史。

准确边界：最后复核之前已被 Crisp 接收的网络请求无法撤回；远端超时结果未知先对账，不承诺跨系统绝对 exactly-once。自检错误 Secret 401 必须匹配本项目 Code 错误结构，但仅证明执行/拒绝路径，业务成功另有真实工作流隔离消息证据。

## 11. 分层计数与重现

核心验收 commit `750e5a18fae987a143482abb51b7e9e52af39af1` 在独立 Debian 12 VM 的干净 all-refs clone 中执行完整发布模式，57 通过、0 失败、1 跳过，进程退出 0。实际命令为本节下列带 `AI_SUPPORT_RELEASE_TEST=1` 的主驱动；UTC 起止为 2026-09-07 09:26:28 至 09:39:19。主 Agent 直接读取机器日志和退出回执，未仅采信 Agent 摘要。

| 层级 | 通过 | 失败 | 跳过 |
| --- | ---: | ---: | ---: |
| STATIC | 8 | 0 | 0 |
| UNIT/CONTRACT | 48 | 0 | 0 |
| REAL-LOCAL | 1 | 0 | 0 |
| EXTERNAL-E2E | 0 | 0 | 1 |
| 合计 | 57 | 0 | 1 |

唯一跳过是缺真实 Crisp/Provider/公网账户资源的外部 E2E，不是在线分发或本地核心豁免。报告冻结后的最终提交完整复验、正式包和匿名空机实装由主 Agent 继续执行，其实际结果记录在独立发布回执；以上已发生的核心验收数字不预先冒充这些后续结果。

模块内部计数单列，不重复累计到主驱动：在线获取 18、管理 PTY 38、runtime 在 B15 后为 23（新增图片记忆/竞态两组）、公开分发本地 6；doctor 完整专项 32 组（包含入口时序新增 2 组），生命周期选定子集 35。它们不等于同数量的真实外部 E2E。先前 51/0/2 基线以及多轮失败/重跑均不相加；沙箱 EPERM 绑定端口失败按正常授权重跑，不删除断言。

开发仓库可复现：

```bash
bash tests/test_get.sh
bash tests/test_doctor.sh
python3 tests/test_manage_ui.py
bash tests/test_legacy_rollback.sh
node n8n/build-workflow.js --check
bash tests/test_static_security.sh
AI_SUPPORT_INTEGRATION_DEPLOY_DIR=/绝对路径/隔离测试实例 AI_SUPPORT_RELEASE_TEST=1 AI_SUPPORT_EXTERNAL_VALIDATION_PENDING=1 bash tests/run.sh
bash tests/test_public_distribution.sh --remote
```

这些是维护者测试命令，不是新用户安装前提。真实故障注入脚本只允许在本项目显式隔离的 VM 运行，不提供给用户对正在接待客户的实例盲目执行。

## 12. 本轮验收矩阵索引

以下 UNIT 简写包含契约/协议 fixture；REAL 指真实本地组件；PUBLIC 指正式发布后无认证分发与空机实装。所有 PUBLIC 最终状态、资产 hash 以独立回执为完成依据。

| ID | 场景 / 实际验证入口 | 层级与证据 |
| --- | --- | --- |
| I01 | 文档原样一行安装 | PUBLIC：22246 最终回执；不拿已登录下载代替 |
| I02 | 单文件无邻接模块 | UNIT get；REAL get→完整包→install |
| I03 | 无 curl/CA；现成工具不重装 | UNIT 推荐命令 fixture；PUBLIC 新 VM 无 curl/wget |
| I04 | 无 Docker/Compose/jq 空机 | REAL 22244 自动依赖；PUBLIC 22246 最终实装 |
| I05 | root/sudo/秘密输入 | REAL root PTY；PUBLIC sudo 及 echo/脱敏断言 |
| I06 | 文件 10/0、多行扩展 | REAL PTY 10/0；UNIT wizard/menu 逐字粘贴；REAL Prompt 回读 |
| I07 | 无 TTY/管道/EOF/中断 | UNIT get/wizard/menu；无完整落盘脚本不执行 |
| I08 | 陌生 cwd / 相对资料路径 | REAL 中文空格文件；PUBLIC 相同路径 fixture |
| I09 | HTTP 错误/HTML/断流/超时 | UNIT get 真实 HTTP 服务，有界失败且无包内执行 |
| I10 | 坏 checksum/归档/gzip | UNIT get/archive；STATIC 最终包安全检查 |
| I11 | Latest 切换期间锁定 tag | UNIT 获取元数据后换 Latest，资产仍按原 tag |
| I12 | 显式版本/非法 tag/资产未齐 | UNIT 正式 metadata、size/state 与路径限制 |
| I13 | 重复/旧版/中断/保留资料 | UNIT get；REAL 0 输入续装与生命周期 |
| I14 | 锁/空间/临时目录 | UNIT 配置锁重入/竞争、下载与快照空间、清理范围；REAL 失败后数据保留 |
| I15 | local-ready 不反复回滚 | UNIT 门禁退出映射；REAL 本地完成退出2、后续升级成功 |
| D01 | doctor/菜单 2 正常结果 | UNIT PTY；REAL 当前组件/时间/摘要；缓存有标识 |
| D02 | daemon 不可达/权限 | UNIT Docker fixture：故障与下游未检分离 |
| D03 | 分别停止四组件 | REAL 故障注入及恢复，服务逐项定位 |
| D04 | running/unhealthy/restarting/OOM | UNIT 状态 fixture；不以 running 判 PASS |
| D05 | DB 端口开、密码错误 | REAL pg_hba 核对、backend SELECT1 错误/恢复，n8n 运行代独立检查 |
| D06 | Anything Key/工作区/Prompt/索引 | UNIT 工作区/缺映射/pending；REAL 错 Key、Prompt 偏离 |
| D07 | active 但 Code/runner 错误 | REAL 修改并发布实际 Code，健康与执行分别失败，恢复后复验 |
| D08 | Provider 200错误/空/429/协议 | UNIT Provider/doctor；REAL Chat-only / Responses-only 最终调用 |
| D09 | Crisp 新凭据、旧观察 | UNIT 七字段绑定/401/403；不引用旧成功 |
| D10 | 本地可达不等于公网收发 | UNIT 分层事实；EXTERNAL 真正 DNS/TLS/Crisp 投递待账户资源 |
| D11 | 停用/永久人工/外部反代 | UNIT 不误判；REAL 14项状态保护，Caddy未启用为SKIP |
| D12 | 扫描/worker 停滞与无流量 | UNIT 滞留 worker；REAL 受阻心跳失败、恢复后无客户流量自行刷新 |
| D13 | 单项/总截止/JSON/非零 | UNIT 真实超时、32组doctor、38组PTY；REAL PTY探针修复 |
| D14 | 默认前后业务比较 | REAL digest/容器身份；仅写诊断元数据，无客户消息 |
| D15 | 安全 fix 前后 | REAL 停止 adapter 后显式修复；staged 拒绝启动混合代 |
| D16 | 诊断导出脱敏 | UNIT 秘密值断言/JSON 白名单；REAL 导出仅分项摘要 |
| R01 | v1.1.0→v1.1.1 | REAL 冻结旧实例升级；PUBLIC 固定新包/入口另验 |
| R02 | 故障回滚/手动/再升级 | REAL 完整循环；UNIT pg_restore/角色密码故障保持停止 |
| R03 | A人工/B AI/计时/总开关 | REAL 生产 n8n 隔离出站与重启；UNIT假时钟/乱序/代次 |
| R04 | Prompt/三库/Responses/图片 | REAL workspace、检索、最终协议/图片请求；UNIT安全URL/格式失败 |
| R05 | 数字卸载/重装/完整清理 | REAL 14项保护/外部备份；UNIT取消与占用网络不误删 |
| R06 | 宽/窄/dumb/拉镜像 | UNIT管理PTY/bootstrap；REAL 首装及复用 ANSI=0 |
| R07 | 空知识/pending/真实失败 | UNIT逐文档映射、有效pending与无关pending三分；不删除索引 |
| P01 | 当前/历史/旧资产/日志扫描 | STATIC 独立审查范围见§6，冻结前再扫描当前/完整历史/包 |
| P02 | public 与未来策略 | 当前 ADMIN、isPrivate=false；活跃 AGENTS/RELEASE 不再要求 private |
| P03 | 无认证 repo/raw/Release/资产 | PUBLIC --remote + 空白用户安装；禁curl配置/netrc/Cookie/认证头 |
| P04 | tag/main/get/下载 SHA | PUBLIC 固定tag资产、raw字节、版本与独立回执 |
| P05 | 公共命令匿名空机实装 | PUBLIC 22246 原样命令，非源码函数模拟 |
| P06 | 文档/帮助/编号一致 | STATIC public-local 6组、菜单PTY；三处命令逐字一致 |
| P07 | 正式 Latest 与回执 | PUBLIC Release非draft/非prerelease/Latest，附件独立核验 |

## 13. 版本、资产与公开后核验

核心修复先验收至 `750e5a18fae987a143482abb51b7e9e52af39af1`；candidate6 包 SHA-256 为 `b323cfd2ddf1399ef585efee7a5da76c00da6cf04a2c7dbe347a0a7d2da6346b`。之后报告冻结 `4ca7578` 在主 Agent 的独立真实 VM 完整复验仍为57/0/1；但最终证据交叉核对又发现 B15 图片上下文缺口，故该提交及包不发布。B15 后重建工作流、归档并重新执行核心门禁，最终验收 commit 与结果记录在独立回执，不能沿用旧生产树相同的假设。最终源码及包以 annotated `v1.1.1^{commit}` 和 Release 的 SHA256SUMS 为准。

固定清单包含 get.sh、doctor/healthcheck、全部原有生产模块、模板和文档；不带 .git/.work、真实配置/知识、日志、数据库或备份。打包只接受干净跟踪内容，固定 mtime/owner/gzip 参数；正式资产为 `ai-support-v1.1.1.tar.gz`、`SHA256SUMS` 与 `get.sh` 审计副本。

按一次正常 main+annotated tag 推送、draft 组装资产、核验后发布 Latest 的流程执行，不 force、不覆盖旧 tag。发布地址固定为 [v1.1.1](https://github.com/statusX7/ai-support/releases/tag/v1.1.1)；该链接的真实存在性、最终 tag commit、public 状态、下载 SHA、命令实装和精确时间必须由同 Release 的 `v1.1.1-verification.md` 实际成功回执确认，不能凭本源码报告中的目标链接推断发布成功。

## 14. 外部接入边界与文档

真实 Crisp 账户、第三方模型 Key、公网 DNS/TLS/Crisp 投递和用户站点编辑权限若未提供，继续标记 `External Validation Pending`。本地应用正常不等于已接待真实访客；无效凭据保持明确失败或 local-ready，不伪造 ready。

新用户只执行 INSTALL 的一行命令。装好后使用 `crispai`、`crispai status`、`crispai doctor`；菜单 14 更新、18 数字卸载。Crisp 后台仍需取得自有凭据并登记生成的生产 Hook，至少订阅 message:send、message:received、message:updated；需要页面加载欢迎/自动打开时按 CRISP 文档接入无密钥 SDK。

文档：[INSTALL](../INSTALL.md)、[MENU](../MENU.md)、[CRISP](../CRISP.md)、[CONFIG](../CONFIG.md)、[TROUBLESHOOTING](../TROUBLESHOOTING.md)、[SECURITY](../SECURITY.md)、[TESTING](../TESTING.md)、[RELEASE](../RELEASE.md)、[可选高级维护](../ADVANCED.md)。新手路径不要求登录 GitHub、上传服务器、手工解压或手装 Docker/Compose/jq/Python。
