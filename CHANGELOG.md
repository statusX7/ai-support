# 更新日志

## v1.2.0

日期：2026-09-07

- 修复实机受管 Caddy 路由被通用 404 抢先处理的问题；仅迁移已识别的旧受管模板，保留自定义反代和非公开后台路径保护。
- 修复真实 Crisp 拒绝旧自动出站自定义 properties 字段的问题，保留官方 automated 标记和持久 fingerprint；明确拒绝与结果未知分别记录，有界处理，不重复生成答案。
- 统一 Website/Plugin 的 Crisp 认证、配对和响应校验；修复未启用 Hook Secret 的容器缺省值导致当前观察事实不能被自检识别的问题。
- 保留正式 crispai 命令，增加不抢占外来程序的受管 crisp 兼容入口，以及在线 get.sh --repair 同版本入口救援。
- 增加 crispai apply 和资料状态查看，统一菜单、直接编辑与运行时的校验、有效投影、应用回读及失败恢复；保留原有资料路径和会话状态。
- 统一 Prompt UTF-8 字节、递归知识来源、库数量、文档和多级菜单限制，补齐父目录链接、非法编码、越界和半写入负例；默认业务备份包含命名知识库全部原文。
- 日志菜单接入来源和容量、限量查看、可中断跟随、数字确认轮转/清理、策略设置及脱敏导出；系统级受管调度不依赖临时维护账号。文件按天、容器按容量、n8n execution 按应用策略分别管理。
- 自检增加资料未应用、入口归属、日志轮转与清理调度检查；默认不修改客服开关、人工期限、知识或任务。
- 更新从十项初始化到第一条真实回复的新手教程，补充 Crisp 多语言定位词、域名/Cloudflare/HTTPS、资料应用和日志维护；现场与发布证据分别见本版报告及独立回执。

## v1.1.1

日期：2026-09-07

- 新增独立 get.sh 和唯一推荐在线安装命令；自动补齐获取工具，锁定正式 tag、校验完整包并进入原有十项中文向导，保留调用目录、隐藏输入与断点恢复。
- 菜单 14 接入同一匿名在线更新逻辑，保留离线与快照回滚；按版本验证旧包必需模块，兼容 v1.1.0 的保留资料恢复。
- 菜单 2 改为状态与自检，支持 doctor 的 local/full/json/last/fix；默认不执行推理、不改变业务状态，完整检查和安全修复由管理员显式触发。
- 自检覆盖实际容器健康、数据库应用认证、AnythingLLM Prompt/索引、n8n 生产 Code、Provider adapter、当前凭据绑定的外部观察和持久任务；提供有界超时、分项事实、脱敏导出与准确退出码。
- 修复旧普通诊断会隐式调用模型、更新菜单仍依赖手工解包/旧私有提示及在线旧版恢复模块校验不兼容；新增生产入口和故障注入回归。
- 修复真实终端中 timeout 与 Docker stdin 转发导致探针被 SIGTTIN 挂起；区分工作流规范化代码与原文件校验，严格核对当前部署代和已发布 Code。
- 修复安装中断时重复 SIGINT 抢先终止收尾的问题；恢复提示完整输出、进度保留，并稳定返回 130，不把中断当作安装成功。
- 数据库探针经实际 backend 网络认证，避免容器 loopback 的 trust 规则掩盖错误密码；知识逐文档核对 hash/location，缺映射失败、有效 pending 显示处理中。
- 修复已有实例旁使用自定义命令路径安装时，自检在入口登记前误查默认路径的问题；最终入口创建仍严格检查所有权，不覆盖其他实例。
- 修复后续追问丢失未在公开答案复述的图片细节：按会话受限保存并关联近期附件，最多 3 份/每份 2000 字符/24 小时，不存图片字节或 URL；迟到视觉请求仍受人工与总开关代次拦截。
- 新版恢复完成前复用统一自检门禁；旧快照不要求不存在的新入口，失败恢复仍保持停止并保留成套材料。
- 发布流程保持现有仓库 Public，审查历史与附件；新手文档改为匿名一条命令，高级离线操作移至独立文档。真实外部接入证据以本轮报告/回执为准。

## v1.1.0

日期：2026-09-06

- 安装受管全局 `crispai` 命令，提供 18 项中文 Emoji 双栏菜单与窄屏/无颜色降级；所有配置入口连接到校验、原子保存、运行组件应用及回读。
- 保留十项初始化和自动 Docker 引导；新增 Prompt/知识多行粘贴，补齐 Python YAML 与 `cmp` 的首次使用前引导。新装默认客服与欢迎启用、自动展开关闭、人工恢复 1800 秒，升级保留合法自定义值。
- 修复 Docker 测试镜像首次拉取在 `TERM=dumb` 仍输出光标动画；捕获非 TTY 诊断且失败保留真实退出码，继续兼容已有 Docker CLI。
- 人工关键词改为原生 picker 确认：仅展示不暂停，真正点击才暂停当前会话。支持缺少 from/type 的 `message:updated`、一次消费、过期/跨会话/旧代次拒绝。
- 真人公开回复立即持久化人工状态；全部自动出站标记与记录自身身份，发送前复核全局与会话代次。按会话保存秒数截止时间，5 秒扫描结合消息到达校验，0 表示永久人工；全局停用保持 Hook 和人工控制接收。
- 新增多个命名知识库的创建、正文/文件维护、启停、增量同步、单库重建和真实检索对账；旧单目录幂等迁移，跨库同名文件独立，停用库退出新检索。
- 用随包部署的受限适配器连接 AnythingLLM Generic OpenAI 与实际所选 Chat/Responses 协议；Provider 候选失败回退并使旧在途任务失效。图片经域名/DNS/类型/大小/像素检查后用于视觉解析与现有知识问答。
- 欢迎、多级菜单、反馈、标签和知识统计复用同一控制状态。提供无密钥网页 SDK 片段，区分发送欢迎与打开聊天框，不误用 operator 的 opened 事件。
- 业务迁移包含所有库原文并排除秘密；完整本机快照包含 `.env`、数据库和人工状态。修复嵌套 Shell 维护锁重入及恢复后容器读取权限，保留目录交换后重建挂载和外部容器占网保护。
- 冷恢复同步 PostgreSQL 角色密码；恢复中断后停止应用并保留成套恢复材料，不启动混合数据。Provider 保存等待真实工作流可用，模型列表临时错误提供有界重试和返回修改；修复配置入口重入与纯文本终端的 JSON 控制字符。
- 加固 JSON/旧 YAML 嵌套敏感字段检查、归档验证、统计保留期限、未确定发送的 fingerprint 对账；部署事实绑定当前凭据，协议服务不能伪造真实 Crisp 会话通过。
- 更新逐菜单说明、私有 Release 获取、Crisp 凭据/Hook/网页接入和分层验收文档。真实外部 Crisp、第三方模型与公网 DNS/TLS 以报告及发布回执为准，未取得资源的项目明确为 `External Validation Pending`。

## v1.0.1

日期：2026-09-05

修改：

- 修复首次安装入口在自动引导前因缺少 Docker、Compose、jq 等命令直接退出的问题；新增 Debian 12/13、Ubuntu 22.04/24.04 的两级依赖引导、Docker 官方软件源安装、daemon 启动和真实测试容器验证。
- 新增由 `install.sh` 直接调用的十项中文快速初始化，覆盖 Provider 模型发现与实际能力探测、Crisp 凭据、公网 Webhook、Prompt、知识文件以及脱敏确认，并支持 0600 状态断点续跑。
- 自动创建或复用 AnythingLLM Developer API Key 与工作区，同步 Prompt 和知识索引，导入并验证已发布的 n8n 生产 workflow；本地应用完成与 Crisp 外部接入状态分层记录。
- 增加可选受管 Caddy HTTPS 入口和已有反向代理分流，切换模式时只清理本 Compose 项目的 Caddy；Provider loopback 地址自动映射到容器宿主网关。
- 重组十项中文管理菜单，接通快速初始化、依赖修复、配置、知识、统计、备份恢复、更新回滚与数字双确认卸载。
- 加固 `.env` 特殊字符序列化、请求凭据传递、向导临时凭据清理、升级自举兼容和仅从干净已跟踪固定清单构建的可复现 Release 归档。
- 安装依赖与拉取容器镜像默认使用无颜色、非动画的稳定输出，避免 `TERM=dumb`、日志采集或窄终端产生控制序列和超大转录。
- 修复 AnythingLLM 首次下载本地 Embedding 模型时索引请求超时与服务端延迟提交的竞态；索引前写入可恢复清单，结果未知时有界对账且不删除在处理文档。
- 为 n8n workflow 导入、发布状态导出增加 15 分钟执行上限和明确失败信息；恢复安装复用既有 AnythingLLM Developer API Key 时不再误报为新建。
- 修复低速首次启动时 n8n JavaScript task runner 的 grant token 过期和任务 60 秒超时；生产 Webhook 现在能完成 Code node 校验，并为当前固定版本同时配置兼容 Webhook URL。
- 将安装、更新、恢复和回滚的本地服务健康等待改为受限且可配置的统一总秒数策略，HTTP 探测耗时计入截止时间，避免慢速但受支持的虚拟机在数据已恢复后误报回滚失败或等待上限不透明。
- 修复更新或回滚中断留下 `installing` 状态时重复要求十项输入的问题；实际配置完整且校验通过时直接复用密钥和业务数据继续安装。
- 修复 localhost Provider 转换成 `host.docker.internal` 后被恢复校验误判的问题；校验原始探测地址与容器运行地址的严格映射，不放宽任意明文 HTTP 主机。
- 修复版本回滚通过目录交换恢复 AnythingLLM 数据后仍复用旧 bind mount inode 的问题；成功或失败恢复旧目录后均重建 AnythingLLM 容器以重新绑定正确数据树。
- workflow 发布后新增生产 Webhook 运行烟雾检查，必须由 n8n task runner 返回预期 401 JSON；回滚完成状态也按真实 Crisp/Webhook 结果写入 `ready` 或 `local-ready`，不再出现 `ready` 与 `fact_crisp_api=failed` 冲突。
- 回滚使用当前进程已完成的服务、Prompt 与 workflow 检查，不再用恢复后的旧版健康脚本解释旧配置 schema，保持跨版本回滚兼容。
- 修复管理菜单调用完整清理时，`--numeric-confirm` 在 `set -u` 下把字符串模式当作算术变量而提前退出的问题；新增数字二次确认的生产入口回归。
- 修复 `wizard.sh` 被安装器加载时信号处理未生效的问题；`sudo bash install.sh` 收到 Ctrl+C/TERM 现在会明确说明断点可恢复，并以非成功状态结束。
- 修复从 v1.0.0 或其快照升级时调用旧部署 `backup.sh` 的兼容问题；更新器优先使用目标版本的备份实现，避免旧状态解析器拒绝 `local-ready` 后才触发回滚。

影响：

- 在受支持的新服务器上，普通路径只需运行 `sudo bash ./install.sh` 并完成十项输入；缺少的普通运行依赖与 Docker Engine/Compose 由脚本自动补齐，无需手工配置数据库、AnythingLLM 或 n8n。
- 本地服务与应用初始化成功但 Crisp 账户、DNS 或后台 Hook 尚未就绪时，部署保留为 `local-ready` 并明确列出最小外部动作，不再误报已开始接待客户。

## v1.0.0

日期：2026-09-05

修改：

- 完成 Crisp、n8n、AnythingLLM 与 OpenAI Compatible API 组合部署框架的正式源码版本，统一安装、管理、更新、回滚、备份恢复和安全卸载入口。
- 将源码发布与部署方外部实例验收分离；新增显式 `External Validation Pending` 发布模式，仅允许 `INTEGRATION` 层因外部资源未配置而跳过。
- 同步应用与 n8n 出站消息版本，增加 workflow 版本一致性检查。
- 修正 `update.sh --help` 对已禁用 `--skip-start` 的误导说明，并避免测试参数校验失败时遗留临时目录。
- 增加正式 release notes、发布报告和无 GitHub 权限时的本地 tag/后续推送流程。

影响：

- 部署方可从正式源码运行 `sudo ./install.sh`，配置自己的 Crisp、Provider 和公网 Webhook 后完成实例健康检查。
- 本版本的本地代码、真实 Docker/AnythingLLM/Provider 历史验收与安全测试结果可追溯；缺少专用 Crisp 账号的端到端项目明确标记为 `External Validation Pending`，未伪造为通过。

## v0.8.2

日期：2026-09-05

修改：

- 安全卸载在 `docker compose down` 后按 Compose 项目标签复核容器和网络；外部 Provider 占用网络导致资源残留时会在删除程序前中止，且不会强制删除外部容器。
- 安全卸载后的同路径重装会先校验保留 `.env` 的权限、必需字段、配置一致性与实际业务配置；有效配置直接复用，缺失、占位或损坏配置明确要求 `--reconfigure`。
- 外部端到端测试只接受本轮新增后的最新 AI 回复，避免旧历史回答造成上下文假阳性；图片验收新增唯一事实标识，必须真实识别图片内容且不得返回视觉降级提示。
- 扩展卸载、保留配置恢复和残留网络回归，并同步安装、安全与发布测试说明。

影响：

- 已在项目内真实 Docker Engine 29.8.0 与 Compose 5.5.1 上完成容器启动、健康、重启持久化、AnythingLLM 四格式知识、OpenAI Compatible Chat/Responses、真实视觉模型、更新故障自动回滚、安全卸载和恢复验收。
- 尚无专用 Crisp Website、凭据、空白 conversation 和公网 Hook，因此 Crisp 真实端到端与正式发布门禁仍未验收；本版本只创建本地候选提交，不创建 `v1.0.0` tag、不 push、不发布 GitHub Release。

## v0.8.1

日期：2026-09-05

修改：

- 默认卸载改为可恢复的安全卸载：自动生成不含密钥的迁移备份，删除容器、网络、Compose 定义和程序文件，保留 `.env`、配置、知识库、运行数据、备份和日志。
- 完整清理始终要求普通确认和精确输入 `PURGE`，`--yes` 不能绕过；清理前的不含密钥备份写入部署目录同级。
- Docker daemon、Compose 或 `docker compose down` 失败时在文件清理前中止，避免容器尚在运行时破坏数据。
- `manage.sh` 主菜单收敛为指定的 11 项，状态和统计分别提供子菜单，安全卸载与完整清理由统一卸载入口选择。
- 新增管理菜单契约测试，并扩展卸载回归，覆盖自动备份、保留边界、双重确认、Docker 失败原子性和输出说明。

影响：

- 默认卸载后可从新的可信源码目录对原路径重新执行 `install.sh`，复用保留的数据与密钥；保留的 `.env` 仍是敏感资产，禁止上传或公开。
- 当前宿主机仍没有 Docker CLI/daemon、真实 Crisp/AnythingLLM/Provider 验收环境和 GitHub remote；本版本只创建本地候选 commit，不创建 `v1.0.0` tag、不 push、不发布 Release。

## v0.8.0

日期：2026-09-05

修改：

- 安装流程改为两阶段状态提交，增加维护锁、输入边界、Docker daemon、AnythingLLM 自动登录、Developer API Key、工作区、Prompt 和已发布 workflow 验证。
- Provider 检测改为实际校验 Chat Completions、Responses 与图片输入的成功响应结构，拒绝 HTTP 200 但携带错误或空结果的伪成功响应。
- Crisp Website Hook 与 Plugin Hook 使用独立 Secret；修正事件方向、签名校验、精确转人工、operator 接管、标签并集、发送前失败关闭复核及稳定出站 fingerprint。
- 同一 conversation 的 AI 开关、接管代次、菜单、欢迎状态和去重指纹写入受限持久化控制文件，并增加并发锁、原子更新、过期与容量清理。
- AnythingLLM 固定使用 `chat` 会话模式；知识库同步增加新索引验证、旧索引回退、源文档清理和失败后的可重试垃圾清单。
- 版本快照升级为包含 n8n PostgreSQL 逻辑备份、AnythingLLM 数据及本机镜像 ID 的一致性格式；更新前拉取、容量检查、历史保留和失败回滚路径得到加固。
- 备份恢复增加完整文件集合校验、重复路径、路径穿越、链接、特殊文件与敏感 Provider 字段防护；卸载在 Docker 停止失败时默认安全中止。
- 新增分层测试、n8n Code node 行为测试、真实部署检查、显式凭据外部 E2E 和严格 `v1.0.0` 发布门禁文档。

影响：

- 本地静态、工作流运行时、Compose 解析和桩集成回归均已通过；当前环境没有 Docker daemon 和专用外部凭据，真实容器与 Crisp 端到端验收仍未完成。
- 本版本仅创建本地候选 commit，不创建 tag、不 push、不发布 GitHub Release；全部真实门禁通过后才允许提升为 `v1.0.0`。
- 默认镜像固定为 n8n `2.33.0`、AnythingLLM `1.16.1` 和 PostgreSQL `16.10-alpine`；部署方升级前必须保留一致性快照。

## v0.7.0

日期：2026-09-04

修改：

- 安装、更新、回滚与管理操作增加 Docker daemon 可连接性检查，安装完成前验证容器、本地接口及已配置的外部服务。
- 更新前增加版本快照容量预检；快照创建时再次校验空间，并在清单中记录容量依据。
- 增加可配置的快照最小剩余空间与历史保留数量策略，回滚期间保护当前目标快照不被清理。
- 转人工默认提示调整为“正在为您转接人工客服，请稍候。”，默认关键词增加“真人”。
- Crisp 默认标签统一为 `ai_resolved`、`knowledge_miss`、`low_confidence`、`human_required`，升级时只迁移旧的项目默认值并保留自定义值。
- 扩展自动测试和真实环境验收说明，明确区分桩测试、Docker daemon 验证与 Crisp 端到端验收。

影响：

- 空间不足会在停止服务前中止升级，默认预留 1024 MiB 并最多保留 10 份有效版本快照；设为 `0` 可关闭对应限制。
- 历史快照含 AnythingLLM 运行数据，自动清理不可恢复，部署方应按容量和审计要求调整保留数量。
- 当前宿主机缺少 Docker 与真实凭据，正式 GitHub push 和 Release 须等待真实环境验收完成。

## v0.6.1

日期：2026-09-04

修改：

- 人工接管仅由真实 operator 回复或用户配置化明确请求触发，知识库未命中、低置信度和 AI 失败不再关闭 AI。
- 增加配置化 Crisp conversation 标签，并在更新标签时保留非项目管理的既有标签。
- 增加匿名知识库命中统计与回答质量反馈，管理菜单可查看命中率、好评率和高频失败问题。
- 增加更新前本地版本快照、版本历史、手动回滚、镜像引用恢复和更新失败自动回滚。
- 增加 `v0.6.0` 人工接管配置的幂等迁移，避免保留旧配置时产生错误提示或健康检查失败。
- 扩展备份恢复、健康检查和自动测试，覆盖新增配置与回滚路径。

影响：

- AI 默认优先响应，人工接管边界更明确；业务关键词不再隐式转人工。
- 版本快照包含 AnythingLLM 运行数据，必须保留在受限的本机目录，禁止上传。
- 本次为日常补丁版本，仅创建本地 Git commit，不执行 GitHub push 或 Release。

## v0.6.0

日期：2026-09-04

修改：

- 增加安装、重复安装、更新、卸载、备份恢复与失败路径自动测试。
- 增加 Webhook 校验、图片消息、关键词、多级菜单、上下文与人工接管契约测试。
- 增加可选的 Node.js 工作流行为测试和 Docker Compose 实际配置检查。
- 强化备份密钥检查与知识文件名安全边界。

影响：

- 核心部署流程可使用无真实凭据的本地桩服务重复验证。
- 缺少 Docker 或 Node.js 的开发环境会明确报告跳过项，不会误报为已验证。

## v0.5.0

日期：2026-09-04

修改：

- 增加幂等安装、中文管理、更新和安全卸载脚本。
- 增加 Provider 模型列表、Responses API 与 Chat Completions API 自动检测。
- 增加 Prompt 导入导出、知识库同步与重新索引功能。
- 增加不含密钥的配置备份、路径安全校验与恢复功能。
- 增加服务、Provider、Crisp 与配置健康检查。

影响：

- 可通过统一管理入口维护部署，不依赖 Codex 运行。
- 所有持久化数据默认集中在 `/opt/crisp-ai`，敏感值保留在权限受限的 `.env`。

## v0.4.0

日期：2026-09-04

修改：

- 增加 Crisp Website Hook、Plugin Hook 签名验证与 REST API 回复工作流。
- 增加用户消息过滤、事件去重、图片理解、连续会话和人工回复上下文。
- 增加关键词、多级菜单、欢迎语与人工接管配置模板。
- 增加低置信度、知识库无答案及敏感主题自动转人工规则。

影响：

- n8n 可编排完整客服消息链路，并阻止机器人回复触发循环。
- Website Hook 使用 URL Secret；Plugin Hook 使用 HMAC-SHA256 签名与时间戳校验。

## v0.3.0

日期：2026-09-04

修改：

- 集成 AnythingLLM、内置 LanceDB 与原生自动分块流程。
- 增加 OpenAI Compatible Provider、客服 Prompt 配置模板。
- 增加知识库目录规范和配置文档。

影响：

- 可通过 AnythingLLM 管理知识文档、检索内容和连续会话。
- API Key 仅保存于被 Git 忽略的 `.env`，不会写入 Provider 配置文件。

## v0.2.0

日期：2026-09-04

修改：

- 增加 Docker Compose 基础编排。
- 增加 n8n 与 PostgreSQL 持久化服务、健康检查和资源保护配置。
- 增加部署环境变量模板、应用基础配置和架构说明。

影响：

- 建立可重复部署的容器运行基础，服务默认仅监听本机地址。

## v0.1.0

日期：2026-09-04

修改：

- 初始化项目目录与 Git 版本管理。
- 增加版本、许可、协作规范和基础说明文件。
- 增加敏感配置与运行数据忽略规则。

影响：

- 建立后续分阶段开发的可追溯基线。
