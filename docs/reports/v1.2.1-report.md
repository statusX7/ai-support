# CrispAI v1.2.1 交付报告

## 版本与证据边界

本报告随候选源码冻结。正式发布、资产 SHA256、匿名回下载及 TARGET-A 最终同包核验，以本版本独立 `v1.2.1-verification.md` 回执中的实际结果为准。未取得成功回执前，不将候选描述为已经发布。

- 实际基线：`3b7f96ed0b95c6d0ffa3779755bcec57a467a952`，已读取 v1.2.0 报告及对应正式 Release 核验附件。
- 仓库：现有 `statusX7/ai-support`，保持 public；不创建替代仓库，不移动历史 tag。
- 本轮目标：v1.2.1。用 `git rev-parse v1.2.1^{commit}` 核对正式代码身份；归档顶层 VERSION、工作流元数据与代码一致。
- 发布门槛：同一冻结包必须先在 TARGET-A 通过候选验收；此前不推送本轮代码、tag、draft 或资产。
- 本轮独立基线回归：STATIC 8、UNIT/CONTRACT 55，共 63 通过、0 失败；REAL-LOCAL 与 EXTERNAL-E2E 各跳过 1，未累加历史报告数字。

## 实际协作

主 Agent 负责公共配置、Compose、推理与取消接线、doctor、资料应用、迁移、快照回滚、集成和发布。执行 Agent 按并发容量分批协作：

| 职责 | 实际产出与所有权 |
| --- | --- |
| A 多接口 | adapter、router、签名信封、接口池管理后端及协议测试 |
| B 中文 UI | manage、展示层、主备菜单、真实 PTY 和 MENU |
| C 评价与独立回归 | 精确退役旧评价状态，保留未知出站对账，配置/生命周期独立测试 |
| D 实机与文档 | 唯一 TARGET-A 操作者，受限现场备份、复现及候选验收，操作手册 |

共享文件由主 Agent 集成，目标机不并发变更；任何 Agent 自述通过均不替代最终集成复验。

## 已确认问题与修复

| 编号 | 文件/函数与复现 | 根因、修复及回归 |
| --- | --- | --- |
| V121-01 | `manage.sh` 欢迎设置 → `configuration_apply`，选择关闭后输出 `enabled=true` | TARGET-A 原文和有效投影均已关闭欢迎，总开关仍为 true；旧回执返回的是 runtime 对象。现在明确返回 target/value，界面分别回读欢迎和总开关，普通菜单用中文说明。配置协议及生产 PTY 同时验证真实状态，不用绿字覆盖失败。 |
| V121-02 | `n8n/runtime.js` 的普通答案发送及旧反馈计划 | TARGET-A 旧自用会话的回答确有本项目追加评价后缀。发送出口不再追加，旧反馈开关显式迁移关闭；独立评价任务/offer 精确退役，普通“是/否”继续咨询。已送达历史和统计保留。 |
| V121-03 | 旧发送结果未知且超过处理窗口，再遇到总开关关闭 | 不应直接丢掉未知出站的对账机会，也不得重新生成或重复发送。恢复路径先作有限 REST 对账；过期/停用/已退役任务不再 POST。专项包含已收与未收两种结果。 |
| V121-04 | 新接口后端的模型列表 429/挂起回归 | 整合时发现原有列表三次有界重试被一次请求取代。恢复仅限只读列表的三次上限和单次 10 秒；模型推理不复用该循环，明确额度不足及较长 Retry-After 不立即重打。 |
| V121-05 | 主备代直接编辑、导入和旧版本回滚 | 新秘密和状态不能只加入运行容器而遗漏资料及快照。完整池源先校验，再原子应用并读回；业务包排除秘密，完整快照含秘密及路由状态；回滚旧版本保留新池的受限恢复副本。 |
| V121-06 | `migration_apply_legacy_provider`，导入旧单接口业务包 | 旧包携带 type/api_key_env 投影元数据，新候选白名单不接受它们。明确转换三元组，不改变本机 Key/Header；旧评价启用同时迁移关闭。生命周期正负例及真实旧 tag 快照回滚均通过。 |
| V121-07 | `log-redact.py` / 日志导出 / Git 忽略规则 | 新备份接口及导入草稿的秘密不在旧 .env 清单中。统一纳入当前、历史和草稿秘密代次及常见编码；follow 在输出前刷新清单，无法完整读取时停止输出。实际实例池、资料投影及秘密目录禁止纳入 Git。 |
| V121-08 | `doctor_render_text` 普通中文输出 | 集成中使用了 jq 保留字作函数名，JSON 检查成功而文本渲染退出。生产安装入口复现后改名，并增加普通缓存显示负例，防止仅测试 --json 遗漏人类入口。 |

## 五项要求的真实实现

| 要求 | 管理入口 | 权威文件与运行消费者 | 验证范围 |
| --- | --- | --- | --- |
| 停止自动评价 | 菜单 11 仅查看历史 | feedback 原文迁移；有效资料投影；runtime 接收/扫描/发送 | F01–F08；新旧会话、未知出站、重启及普通“是/否” |
| 1 主 20 备 | 菜单 3，逐接口模型/协议/排序/角色/草稿 | `config/provider-pool.yaml` → `provider-pool-applied.json`；秘密分代目录；adapter/router | A01–A28；容量、HTTP 错误、共享预算、冷却和取消 |
| 中文交互 | 18 主菜单及子菜单；显式 `--json` 保留 | `manage.sh`、`menu-provider-ui.sh`、`menu-display.py` | U01–U12；PTY 输入/输出、状态回读与失败恢复 |
| 自检及维护 | 菜单 2/15/16；`crispai doctor`、`crispai apply` | 同一 doctor、materials、migration、snapshot/rollback | 配置接线、权限、降级、资料与状态保护 |
| 先实机后发布 | 完整包生产 update 流程 | 冻结提交、包成员、目标机运行文件和独立回执 | R01–R14；未通过则不公开发布 |

## 接口池与费用边界

非敏感定义与秘密分离，稳定 ID 不随改名/排序变化。停用备用仍占容量；设主为角色交换。旧单接口幂等成为唯一主接口，无备用合法，不要求重复填写十项向导。导入缺主凭据时只保存草稿，原有效池继续工作；同 ID 本机秘密不会被空值覆盖。

文本 RAG 与直接视觉均通过同一 adapter。每问题首次确定的预算跨阶段与重试保持：默认总 90 秒、单调用 20 秒、连接 5 秒、总 21 次；不承诺一定等到最后一个备用。正常回答、拒绝和知识不足不作故障切换。Crisp 发送/标签错误不触发重新推理。

接口配置变化、总开关和单会话接管都会作废未发送的旧结果。冷却与管理员停用分离；半开试探有单租约，两次有效成功恢复。状态受限持久保存，不使用新的数据库服务。无后台付费轮询；失败或取消可能仍被上游计费。

上下文使用明确的近似 token 估算，不宣称精确 tokenizer；不静默丢弃知识或图片。不同供应商不会复用响应 ID/文件 ID/线程 ID。实际独立上游数量与 21 条目协议容量分开：TARGET-A 初始只有 1 个真实主接口、0 个真实备用。故障注入仅作用隔离合成任务，不能留作正式备用。

## 本轮证据分类

基线与新增套件分别记录，不将内部子项和主驱动累计相加。Node HTTP 严格协议服务属于 UNIT/PROTOCOL，即使请求真实经过本地 TCP，也不等同真实 AnythingLLM/Crisp/第三方供应商验收。

新增专项包括：接口池 18 组协议场景、runtime 到实际 adapter 的 4 组跨模块协议场景、评价 13 组场景、完整菜单 PTY、主备 doctor、业务迁移与快照。配置协议当前 31 组及生命周期 10 组均通过；这些是套件内部数字，不累加到主驱动。冻结版本的最终计数和 TARGET-A/REAL-LOCAL/PUBLIC-DISTRIBUTION 结论在完成复验后锁定，并由发布回执补充不可自引用的资产事实。

## 隐私与操作说明

TARGET-A 仅使用代号。现场连接、原始日志、配置、知识和会话证据留在受限本地或目标机，不上传。公开程序并不公开客服数据。正式提交与附件扫描包含当前索引、历史新增对象、所有归档成员及公开文本；不把秘密清单本身用作公开测试常量。

新手依旧使用 [INSTALL](../INSTALL.md) 的唯一公开在线命令；主接口在十项初始化中配置，备用安装后从菜单 3 添加。欢迎开关在菜单 9；历史统计在菜单 11；资料应用在菜单 16；日志维护在菜单 15；备份/更新/卸载保持 13/14/18。详细边界见 [CONFIG](../CONFIG.md)、[MENU](../MENU.md)、[SECURITY](../SECURITY.md) 和 [TESTING](../TESTING.md)。

本轮候选阶段尚不声称 TARGET-A 已运行正式 v1.2.1，也不建议据此撤销临时排障账号。最终是否可以撤销，以完成同包验收后的私密交付说明与公开核验回执为准。
