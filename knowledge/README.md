# 知识库目录

把需要纳入客服知识库的 `.md`、`.txt`、`.pdf` 或 `.docx` 文件放入本目录，再通过 `./manage.sh` 执行同步或重新索引。

文件由 AnythingLLM 解析并自动分块，不设置 3000 字限制。请勿放入 Token、密码、私钥、真实用户数据或其他敏感信息。

删除本地文件后仍需执行同步，才能从 AnythingLLM 工作区移除对应文档。
